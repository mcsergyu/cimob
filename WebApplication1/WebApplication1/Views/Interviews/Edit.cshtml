@model Cimob.Models.Interview

@{
    ViewData["Title"] = "Edit";
}

<h2>Editar</h2>

<h4>Entrevista</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="InterviewId" />
            @*
            <div class="form-group">
                <label asp-for="CandidaturaId" class="control-label"></label>
                <select asp-for="CandidaturaId" class="form-control" asp-items="ViewBag.CandidaturaId"></select>
                <span asp-validation-for="CandidaturaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            *@
            <div class="form-group">
                <label class="control-label">Data pretendida</label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            @*
            <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="Confirmed" /> @Html.DisplayNameFor(model => model.Confirmed)
                    </label>
                </div>
            </div>
            *@
            <div class="form-group">
                <input type="submit" value="Agendar" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a href="/Candidaturas/Details/@Model.CandidaturaId">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
